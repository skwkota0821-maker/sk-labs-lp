name: 退役済み確認・LP背景調整

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify-retired:
    runs-on: ubuntu-latest
    steps:
      - name: 正本を読み取り専用で取得
        uses: actions/checkout@v4
      - name: 退役状態を確認
        run: |
          echo "このWorkflowの修正内容は現行LPへ反映済みです。"
          echo "mainへの自動commit/pushは行いません。"
